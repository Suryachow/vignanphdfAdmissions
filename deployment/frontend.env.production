# --- PRODUCTION FRONTEND ENV ---
# Location: Instance 1 (Local Build)

# Because Instance 1 handles the proxying via Nginx,
# the frontend should point to ITS OWN Public IP or Domain.
# Nginx will then forward these requests to the Backend Instance 2.

VITE_API_URL=http://13.204.96.58:8000
# Direct to backend Elastic IP.
# If using Nginx reverse proxy on the same host, you can use "/" instead.
